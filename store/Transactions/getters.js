export default {
    allDeliveries(state) {
        return state.deliveriesState;
        //console.log(state.deliveriesState);
    },
    allSales(state) {
        return state.salesState;
        //console.log(state.deliveriesState);
    }
}