export default {
    allCustomers(state) {
        return state.customersState;
        //console.log(state.customersState);
    },
    allSuppliers(state) {
        return state.suppliersState;
        //console.log(state.customersState);
    },
}